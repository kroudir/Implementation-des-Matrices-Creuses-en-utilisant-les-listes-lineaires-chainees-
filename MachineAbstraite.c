#include "Header.h"

/**<*--------------------------------------------------------------------------- */
/**<*              Les procédures de la Machine abstraite                        */
/**<*-------------------------------------- -------------------------------------*/
Maillon* Allouer ()
{
    return ((Maillon*) malloc( sizeof(Maillon)));
}
//-------------------------------------------------------------------------------------------------
void Aff_Val(Maillon*P, float v)
{
    P->val=v;
}
//-------------------------------------------------------------------------------------------------
int Colone(Maillon *P)
{
    return (P->col);
}
//-------------------------------------------------------------------------------------------------
void Aff_col(Maillon*P,int c)
{
    P->col = c;
}
//-------------------------------------------------------------------------------------------------
void Aff_Adr(Maillon*P,Maillon*Q)
{
    P->suiv = Q;
}
//-------------------------------------------------------------------------------------------------
Maillon* Suivant(Maillon*P)
{
      return( P->suiv );
}
//-------------------------------------------------------------------------------------------------
float Valeur(Maillon*P)
{
    return( P->val) ;
}
//--------------------------------------------------------------------------------------------------
void liberer(Maillon*p)
{
    free(p);
}
//--------------------------------------------------------------------------------------------------
